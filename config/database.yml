# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
development:
  adapter: postgresql
  host: localhost
  username: postgres
  database: lunchbot-dev

test:
  adapter: postgresql
  host: localhost
  username: postgres
  database: lunchbot-test

production:
  adapter: postgresql
  host: localhost
  username: postgres
  database: lunchbot-prod